apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: university-chatbot
spec:
  selector:
    app: frontend
  ports:
    - port: 3000
      targetPort: 3000
  type: NodePort
